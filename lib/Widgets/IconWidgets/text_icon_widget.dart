import 'package:flutter/material.dart';

class TextIconWidget extends StatelessWidget {
  const TextIconWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.text_fields_outlined);
  }
}
