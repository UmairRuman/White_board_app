import 'package:flutter/material.dart';

class ArrowTextWidget extends StatelessWidget {
  const ArrowTextWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Arrow");
  }
}
