import 'package:flutter/material.dart';

class PenTextWidget extends StatelessWidget {
  const PenTextWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Pen");
  }
}
