import 'package:flutter/material.dart';

class ShapeTextWidget extends StatelessWidget {
  const ShapeTextWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Shape");
  }
}
