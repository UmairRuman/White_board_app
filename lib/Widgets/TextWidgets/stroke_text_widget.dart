import 'package:flutter/material.dart';

class StrokeTextWidget extends StatelessWidget {
  const StrokeTextWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Stroke");
  }
}
